The written pdf explaining this sprint is report.pdf 

parser.py and lexer.py contain the parser and lexer respectively.

thcast.py contains classes relating to the AST

sample/compiled contains what the compiled version of test should look like.

data.h and default.c contains default runtime headers which will be included in every
compiled project. test.c contains the actual code for test.thc
